# Supabase

This directory is managed through the official Supabase GitHub integration.

- `migrations/` contains the production database migration history.
- Production schema deployment is handled by Supabase from the `main` branch.
- Historical JSON data import remains a separate manual operation.
